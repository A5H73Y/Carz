sudo: false
language: java
dist: trusty
jdk:
  - oraclejdk8
notifications:
  email: false
cache:
  directories:
    - $HOME/.m2
before_deploy:
  - git config --local user.name "A5H73Y"
  - git config --local user.email "skater.ash@hotmail.com"
  - export GIT_TAG=Carz-SNAPSHOT.$TRAVIS_BUILD_NUMBER
  - git tag $GIT_TAG -a -m "Generated from TravisCI for build $TRAVIS_BUILD_NUMBER"
deploy:
  provider: releases
  github_token: $PAT
  file: "target/Carz.jar"
  skip_cleanup: true
  api_key: $PAT
